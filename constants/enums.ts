export enum Routes {
  Categories = "Categories",
  CategoryMeals = "CategoryMeals",
  MealDetail = "MealDetail",
  Meals = 'Meals',
  Favorites = 'Favorites',
  MealsFavs = 'MealsFavs',
  Filters = 'Filters'
}

export enum PlatformTypes {
  Android = 'android',
  iOS = 'ios'
};
